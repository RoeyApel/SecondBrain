# Notes / Questions

1. hamalId and stationId is currently not an Enum, for it to be an Enum I need to change the db and add another row (storing Enum id) for the in hamal and station tables. Should probably make this change once we make a black Lillit db.