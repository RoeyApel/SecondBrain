1. look at [[PTZ Camera Connection Guide]] to connect the camera 
2. Tilt services needed: `frontend_local` (`-rotem`, or regular if Yanai), `backend_local`, `device-manager-service_local`, `video-server`,`storage`.
