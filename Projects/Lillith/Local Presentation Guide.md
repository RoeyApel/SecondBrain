1. look at [[PTZ Camera Connection Guide]] to connect the camera 
2. Tilt services needed: `frontend_local` (rotem), `backend_local`, `device-manager-service_local`, `video-server`, `storage`.
3. make sure that you run jessi project on port 8000 not 3000
