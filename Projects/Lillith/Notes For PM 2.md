# Notes / Questions

1. hamalId and stationId is currently not an enum, for it to be an enum I need to change the db and add another row (enum id) for the in hamal and station tables. Should probably make this change once we make a black lillit db.